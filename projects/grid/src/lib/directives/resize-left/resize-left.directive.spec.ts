import { ResizeLeftDirective } from './resize-left.directive';

describe('ResizeLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeLeftDirective();
    expect(directive).toBeTruthy();
  });
});
