import { KcOptionsDirective } from './kc-options.directive';

describe('OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new KcOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
