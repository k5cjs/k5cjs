import { KcCloseDirective } from './kc-close.directive';

describe('KcCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new KcCloseDirective();
    expect(directive).toBeTruthy();
  });
});
