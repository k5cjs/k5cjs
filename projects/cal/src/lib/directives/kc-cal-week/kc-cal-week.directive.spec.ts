import { KcCalWeekDirective } from './kc-cal-week.directive';

describe('KcCalWeekDirective', () => {
  it('should create an instance', () => {
    const directive = new KcCalWeekDirective(null!, null!);
    expect(directive).toBeTruthy();
  });
});
