import { MoveDirective } from './move.directive';

describe('MoveDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveDirective();
    expect(directive).toBeTruthy();
  });
});
