import { KcInput } from './kc-input';

describe('InputDirective', () => {
  it('should create an instance', () => {
    const directive = new KcInput();
    void expect(directive).toBeTruthy();
  });
});
