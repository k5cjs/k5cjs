import { KcSelectAllDirective } from './kc-select-all.directive';

describe('KcSelectAllDirective', () => {
  it('should create an instance', () => {
    const directive = new KcSelectAllDirective();
    expect(directive).toBeTruthy();
  });
});
