import { KcCalWeekOutletDirective } from './kc-cal-week-outlet.directive';

describe('KcCalWeekOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new KcCalWeekOutletDirective(null!);
    expect(directive).toBeTruthy();
  });
});
