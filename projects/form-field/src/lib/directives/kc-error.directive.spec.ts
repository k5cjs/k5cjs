import { KcErrorDirective } from './kc-error.directive';

describe('KcErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new KcErrorDirective();
    expect(directive).toBeTruthy();
  });
});
