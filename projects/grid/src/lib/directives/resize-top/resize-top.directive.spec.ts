import { ResizeTopDirective } from './resize-top.directive';

describe('ResizeTopDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTopDirective();
    expect(directive).toBeTruthy();
  });
});
