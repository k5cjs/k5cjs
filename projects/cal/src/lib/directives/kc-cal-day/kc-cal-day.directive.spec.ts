import { KcCalDayDirective } from './kc-cal-day.directive';

describe('KcCalDayDirective', () => {
  it('should create an instance', () => {
    const directive = new KcCalDayDirective(null!);
    expect(directive).toBeTruthy();
  });
});
