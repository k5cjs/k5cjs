import { PreviewDirective } from './preview.directive';

describe('PreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewDirective();
    expect(directive).toBeTruthy();
  });
});
