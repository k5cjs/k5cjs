import { ResizeBottomRightDirective } from './resize-bottom-right.directive';

describe('ResizeBottomRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeBottomRightDirective();
    expect(directive).toBeTruthy();
  });
});
