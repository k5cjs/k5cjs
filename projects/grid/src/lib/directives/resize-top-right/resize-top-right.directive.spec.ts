import { ResizeTopRightDirective } from './resize-top-right.directive';

describe('ResizeTopRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTopRightDirective();
    expect(directive).toBeTruthy();
  });
});
