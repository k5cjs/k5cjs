import { ResizeBottomLeftDirective } from './resize-bottom-left.directive';

describe('ResizeBottomLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeBottomLeftDirective();
    expect(directive).toBeTruthy();
  });
});
