import { KcToggleItemDirective } from './toggle-item.directive';

describe('ToggleItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KcToggleItemDirective(null!, null!);
    expect(directive).toBeTruthy();
  });
});
