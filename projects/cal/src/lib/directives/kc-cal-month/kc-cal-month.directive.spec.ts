import { KcCalMonthDirective } from './kc-cal-month.directive';

describe('KcCalMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new KcCalMonthDirective(null!, null!);
    expect(directive).toBeTruthy();
  });
});
