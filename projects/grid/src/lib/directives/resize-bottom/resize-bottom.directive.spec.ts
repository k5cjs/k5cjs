import { ResizeBottomDirective } from './resize-bottom.directive';

describe('ResizeBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeBottomDirective();
    expect(directive).toBeTruthy();
  });
});
