import { ResizeRightDirective } from './resize-right.directive';

describe('ResizeRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeRightDirective();
    expect(directive).toBeTruthy();
  });
});
