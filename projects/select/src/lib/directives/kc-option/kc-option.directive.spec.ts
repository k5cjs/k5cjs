import { KcOptionDirective } from './kc-option.directive';

describe('OptionDirective', () => {
  it('should create an instance', () => {
    const directive = new KcOptionDirective();
    expect(directive).toBeTruthy();
  });
});
