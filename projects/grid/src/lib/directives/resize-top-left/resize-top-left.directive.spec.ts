import { ResizeTopLeftDirective } from './resize-top-left.directive';

describe('ResizeTopLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTopLeftDirective();
    expect(directive).toBeTruthy();
  });
});
