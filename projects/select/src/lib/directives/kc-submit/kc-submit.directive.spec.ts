import { KcSubmitDirective } from './kc-submit.directive';

describe('KcSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new KcSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
