import { KcDeselectAllDirective } from './kc-deselect-all.directive';

describe('KcDeselectAllDirective', () => {
  it('should create an instance', () => {
    const directive = new KcDeselectAllDirective();
    expect(directive).toBeTruthy();
  });
});
