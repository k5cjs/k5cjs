import { KcClearDirective } from './kc-clear.directive';

describe('KcClearDirective', () => {
  it('should create an instance', () => {
    const directive = new KcClearDirective();
    expect(directive).toBeTruthy();
  });
});
